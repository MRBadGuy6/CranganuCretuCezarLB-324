services:
  - type: web
    name: tagebbbuch
    env: python
    region: oregon    
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    autoDeploy: true
    envVars:
      - key: PASSWORD
        fromSecret: PASSWORD  
