repos:

- repo: https://github.com/psf/black
  rev: 23.9.1
  hooks:
    - id: black
      name: black
      language_version: python3

- repo: local
  hooks:
  - id: pytest
    name: Run pytest
    entry: pytest
    language: system
    pass_filenames: false
    stages: [push]
    always_run: true